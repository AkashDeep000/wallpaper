/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
    images: {
    domains: ['pocketbase.akashdeep000.repl.co'],
  },
}

module.exports = nextConfig
